const u = Array.isArray;
export function isUser(e) {
  if (!(typeof e == "object" && e !== null)) return !1;
  const { id: f, login: r, bio: t } = e;
  if (
    typeof f != "number" ||
    typeof r != "string" ||
    !(typeof t == "object" && t !== null)
  )
    return !1;
  const { first: n, last: s } = t;
  return !(typeof n != "string" || typeof s != "string");
}
export function isPost(e) {
  function f(o) {
    return typeof o == "string" || typeof o == "number";
  }
  if (!(typeof e == "object" && e !== null)) return !1;
  const { title: r, text: t, link: n, published: s, author: l, more: i } = e;
  return !(
    typeof r != "string" ||
    typeof t != "string" ||
    !(typeof n > "u" || typeof n == "string") ||
    typeof s != "boolean" ||
    !isUser(l) ||
    !(u(i) && i.every(f))
  );
}
